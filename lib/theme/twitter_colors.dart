import 'package:flutter/material.dart';

class TwitterColors {
  static const Color background = Color(0xFF15202B);  // Deep blue-gray background
  static const Color cardBg = Color(0xFF192734);      // Lighter background for cards
  static const Color accent = Color(0xFF1DA1F2);      // Twitter blue
  static const Color textPrimary = Color(0xFFFFFFFF); // White text
  static const Color textSecondary = Color(0xFF8899A6); // Gray text
  static const Color iconColor = Color(0xFF8899A6);   // Default icon color
  static const Color divider = Color(0xFF38444D);     // Divider lines
}